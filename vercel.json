{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "bun install && cd apps/web && bun install --frozen-lockfile",
  "buildCommand": "cd apps/web && bun run build",
  "outputDirectory": "apps/web/.next",
  "ignoreCommand": "if [ \"$VERCEL_PROJECT_ID\" = \"prj_EvLi9wcPcy2p7op1ChdvI8kPksKV\" ] || { [ \"$VERCEL_ENV\" = \"preview\" ] && [ \"$VERCEL_GIT_COMMIT_REF\" = \"feat/xpex-v6-002-polo-foundation\" ]; }; then exit 1; else exit 0; fi",
  "github": {
    "autoJobCancelation": true
  }
}
